# yjd

Your job is done when the code is DRY with the rest of the codebase, no regressions were introduced, the code is clean, readable and not overly verbose/defensive, and most importantly "$ cd api && just lint && just test-all-mocked" passes. Run these and fix issues until all checks pass.