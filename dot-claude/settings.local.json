{
  "comment": "Superpowers default settings - symlinked to .claude/settings.local.json. pytest removed from blocklist to allow verification of test results.",
  "command_blocklist": [
    "git commit*",
    "git push*",
    "git pull*",
    "git merge*",
    "git reset*",
    "git rebase*",
    "git stash pop*",
    "git cherry-pick*",
    "git apply*",
    "rm -rf*",
    "sudo*"
  ],
  "bashCommandApprovals": {
    "requireApprovalPatterns": []
  },
  "permissions": {
    "allowGitWrite": false
  }
}
